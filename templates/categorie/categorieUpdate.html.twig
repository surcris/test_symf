{% extends 'base.html.twig' %}

{% block title %}Ajouter catégorie!{% endblock %}

{% block body %}
    {{form_start(form)}}
    {{ form_rest(form) }}
    {{form_end(form)}}

    {% if msg is defined %}
       {{msg}} 
    {% endif %} 
{% endblock %}
